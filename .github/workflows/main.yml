name: Deploy

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: ${7593849d-9a5a-4585-a91c-00127d06cd66}
          heroku_app_name: "cise-speed-2022" #Must be unique in Heroku
          heroku_email: "prunella.poiney@gmail.com"
